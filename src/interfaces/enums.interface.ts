export enum NetworkTypes {
  Business = "Business",
  Hosting = "Hosting",
  Mobile = "Mobile",
  Residential = "Residential",
}

export enum RotationType {
  Time = "time",
  Link = "link",
  Request = "request",
}

export enum RotationTimeType {
  Hours = "hours",
  Minutes = "minutes",
}

export enum VpnType {
  Android = "android",
  Ios = "ios",
  Windows = "windows",
  Macos = "macos",
  Mikrotik = "mikrotik",
}

export enum OrderType {
  Random = "random",
  Created = "created",
  Id = "id",
  Name = "name",
}

export enum OrderDirection {
  Asc = "asc",
  Desc = "desc",
}

export enum PortType {
  Active = "active",
  Archived = "archived",
}

export enum ResponseStatus {
  error = "error",
  ok = "ok",
}
